---
import type { BlockBase } from "../../../lib/schemas/block-schema.types";

type Props = {
  block: BlockBase<"Embed">;
};
const { block } = Astro.props;
---

<div set:html={block.data.embed.html} />
